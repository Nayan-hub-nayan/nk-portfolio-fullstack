🚀 Business Dashboard - Complete Setup Guide
📋 Table of Contents

Prerequisites
Project Structure
Backend Setup
Frontend Setup
Database Setup
Connecting Contact Form
Environment Variables
Running the Application
API Documentation
Deployment Guide
Troubleshooting


📦 Prerequisites
Before starting, ensure you have the following installed:

Node.js (v16 or higher) - Download
MongoDB (v5 or higher) - Download
Git - Download
VS Code (Recommended) - Download

Check installations:
bashnode --version
npm --version
mongod --version

📁 Project Structure
business-dashboard/
├── backend/
│   ├── config/
│   │   └── db.js
│   ├── controllers/
│   │   ├── authController.js
│   │   ├── clientController.js
│   │   ├── projectController.js
│   │   ├── activityController.js
│   │   └── revenueController.js
│   ├── middleware/
│   │   ├── auth.js
│   │   └── validation.js
│   ├── models/
│   │   ├── Admin.js
│   │   ├── Client.js
│   │   ├── Project.js
│   │   └── Activity.js
│   ├── routes/
│   │   ├── authRoutes.js
│   │   ├── clientRoutes.js
│   │   ├── projectRoutes.js
│   │   ├── activityRoutes.js
│   │   └── dashboardRoutes.js
│   ├── .env
│   ├── server.js
│   ├── seed.js
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── services/
│   │   ├── context/
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── .env
│   └── package.json
│
└── README.md

🔧 Backend Setup
Step 1: Create Backend Directory
bashmkdir business-dashboard
cd business-dashboard
mkdir backend
cd backend
Step 2: Initialize Node.js Project
bashnpm init -y
Step 3: Install Dependencies
bashnpm install express mongoose bcryptjs jsonwebtoken dotenv cors joi express-validator morgan
npm install --save-dev nodemon
Step 4: Create Folder Structure
bashmkdir config controllers middleware models routes utils
Step 5: Create .env File
bashtouch .env
Add the following content:
env# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/business_dashboard

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345
JWT_EXPIRE=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
Step 6: Update package.json Scripts
json{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js"
  }
}
Step 7: Create All Backend Files
Copy all the backend code provided earlier into their respective files:

server.js
config/db.js
All models, controllers, routes, and middleware files

Step 8: Start MongoDB
On Windows:
bashnet start MongoDB
On Mac/Linux:
bashsudo systemctl start mongod
# or
brew services start mongodb-community
Step 9: Seed Admin User
bashnpm run seed
Output:
✅ MongoDB Connected
✅ Admin user created:
   Email: admin@admin.com
   Password: Admin@123
Step 10: Start Backend Server
bashnpm run dev
Expected Output:
🚀 Server running on port 5000
📊 Environment: development
✅ MongoDB Connected: localhost

🎨 Frontend Setup
Step 1: Create Frontend with Vite
bashcd ..
npm create vite@latest frontend -- --template react
cd frontend
Step 2: Install Dependencies
bashnpm install
npm install react-router-dom axios lucide-react
npm install -D tailwindcss postcss autoprefixer
Step 3: Initialize Tailwind CSS
bashnpx tailwindcss init -p
Step 4: Configure Tailwind
Update tailwind.config.js:
javascript/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
Step 5: Update index.css
Replace content with:
css@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
Step 6: Create .env File
bashtouch .env
Add:
envVITE_API_URL=http://localhost:5000/api
Step 7: Create Folder Structure
bashcd src
mkdir components services context utils
mkdir components/Dashboard components/Clients components/Projects components/Revenue components/Settings components/Layout components/Auth components/Common
Step 8: Create API Service
Create src/services/api.js:
javascriptimport axios from 'axios';

const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';

const api = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json'
  }
});

// Add token to requests
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// Handle auth errors
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
Step 9: Start Frontend
bashnpm run dev
Expected Output:
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose

🗄️ Database Setup
MongoDB Collections Structure
The system will automatically create these collections:

admins - Admin user credentials
clients - Client information from contact form
projects - Project details and status
activities - Activity timeline logs

Verify MongoDB Connection
bashmongosh
use business_dashboard
show collections
db.admins.find()

🔗 Connecting Contact Form
Existing Portfolio Contact Form Integration
Update your existing contact form to send data to the new backend:
Option 1: Using Fetch API
javascript// In your existing contact form component
const handleSubmit = async (e) => {
  e.preventDefault();
  
  const formData = {
    name: e.target.name.value,
    email: e.target.email.value,
    mobile: e.target.mobile?.value || '',
    message: e.target.message.value,
    projectType: e.target.projectType?.value || '',
    budget: e.target.budget?.value || '',
    requirements: e.target.requirements?.value || ''
  };

  try {
    const response = await fetch('http://localhost:5000/api/clients/contact', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(formData)
    });

    const data = await response.json();
    
    if (data.success) {
      alert('Thank you! We will contact you soon.');
      e.target.reset();
    }
  } catch (error) {
    console.error('Error:', error);
    alert('Something went wrong. Please try again.');
  }
};
Option 2: Using Axios
javascriptimport axios from 'axios';

const handleSubmit = async (e) => {
  e.preventDefault();
  
  const formData = {
    name: e.target.name.value,
    email: e.target.email.value,
    mobile: e.target.mobile?.value || '',
    message: e.target.message.value,
    projectType: e.target.projectType?.value || '',
    budget: e.target.budget?.value || '',
    requirements: e.target.requirements?.value || ''
  };

  try {
    const { data } = await axios.post(
      'http://localhost:5000/api/clients/contact',
      formData
    );
    
    if (data.success) {
      alert('Thank you! We will contact you soon.');
      e.target.reset();
    }
  } catch (error) {
    console.error('Error:', error);
    alert('Something went wrong. Please try again.');
  }
};
HTML Form Example
html<form id="contactForm" onSubmit={handleSubmit}>
  <input type="text" name="name" placeholder="Your Name" required />
  <input type="email" name="email" placeholder="Your Email" required />
  <input type="tel" name="mobile" placeholder="Phone Number" />
  <select name="projectType">
    <option value="">Select Project Type</option>
    <option value="Web Development">Web Development</option>
    <option value="Mobile App">Mobile App</option>
    <option value="UI/UX Design">UI/UX Design</option>
    <option value="Other">Other</option>
  </select>
  <input type="text" name="budget" placeholder="Budget Range" />
  <textarea name="message" placeholder="Your Message" required></textarea>
  <textarea name="requirements" placeholder="Project Requirements"></textarea>
  <button type="submit">Send Message</button>
</form>

🌐 Environment Variables
Backend .env
env# Server
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/business_dashboard

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345
JWT_EXPIRE=7d

# Frontend URL
FRONTEND_URL=http://localhost:5173
Frontend .env
envVITE_API_URL=http://localhost:5000/api

▶️ Running the Application
Development Mode
Terminal 1 - Backend:
bashcd backend
npm run dev
Terminal 2 - Frontend:
bashcd frontend
npm run dev
Terminal 3 - MongoDB:
bashmongod
Access Points

Frontend Dashboard: http://localhost:5173
Backend API: http://localhost:5000
MongoDB: mongodb://localhost:27017

Default Login Credentials
Email: admin@admin.com
Password: Admin@123

📚 API Documentation
Authentication Endpoints
MethodEndpointAccessDescriptionPOST/api/auth/loginPublicAdmin loginGET/api/auth/mePrivateGet current adminPUT/api/auth/change-passwordPrivateChange password
Client Endpoints
MethodEndpointAccessDescriptionPOST/api/clients/contactPublicSubmit contact formGET/api/clientsPrivateGet all clientsGET/api/clients/:idPrivateGet single clientPOST/api/clientsPrivateCreate client manuallyPUT/api/clients/:idPrivateUpdate clientDELETE/api/clients/:idPrivateDelete clientPOST/api/clients/:id/notesPrivateAdd note to client
Project Endpoints
MethodEndpointAccessDescriptionGET/api/projectsPrivateGet all projectsGET/api/projects/:idPrivateGet single projectPOST/api/projectsPrivateCreate projectPUT/api/projects/:idPrivateUpdate projectDELETE/api/projects/:idPrivateDelete project
Dashboard Endpoints
MethodEndpointAccessDescriptionGET/api/dashboard/overviewPrivateGet dashboard statsGET/api/dashboard/revenuePrivateGet revenue analytics
Activity Endpoints
MethodEndpointAccessDescriptionGET/api/activitiesPrivateGet activity timeline

🚀 Deployment Guide
Backend Deployment (Heroku/Railway)

Create .gitignore:

node_modules/
.env
*.log

Update MongoDB URI to production database (MongoDB Atlas)
Deploy:

bashgit init
git add .
git commit -m "Initial commit"
git push heroku main
Frontend Deployment (Vercel/Netlify)

Build production:

bashnpm run build

Update API URL in .env:

envVITE_API_URL=https://your-backend-url.com/api

Deploy:

bashvercel deploy
# or
netlify deploy

🔧 Troubleshooting
Common Issues
1. MongoDB Connection Error
Solution: Ensure MongoDB is running
- Windows: net start MongoDB
- Mac/Linux: brew services start mongodb-community
2. Port Already in Use
Solution: Change PORT in .env or kill existing process
- Find process: lsof -i :5000
- Kill process: kill -9 <PID>
3. CORS Error
Solution: Verify FRONTEND_URL in backend .env matches your frontend URL
4. JWT Token Invalid
Solution: 
- Clear localStorage
- Re-login
- Check JWT_SECRET is same across restarts
5. Module Not Found
Solution: Reinstall dependencies
npm install
Debug Mode
Enable detailed logging:
javascript// In server.js
app.use(morgan('combined'));

📊 Testing the System
Test Contact Form Submission
bashcurl -X POST http://localhost:5000/api/clients/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "mobile": "1234567890",
    "message": "Test message",
    "projectType": "Web Development"
  }'
Test Admin Login
bashcurl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@admin.com",
    "password": "Admin@123"
  }'

🎯 Next Steps

✅ Complete backend setup
✅ Seed admin user
✅ Test API endpoints
✅ Setup frontend
✅ Connect contact form
✅ Test full workflow
🔄 Customize UI/branding
🔄 Add email notifications (optional)
🔄 Setup production deployment


📞 Support
For issues or questions:

Check troubleshooting section
Review API documentation
Verify all environment variables
Check console logs for errors


Made with ❤️ for managing your software services business